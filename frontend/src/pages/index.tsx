import React from 'react';
import WebSocketComponent from '../components/WebSocketComponent';

const Home: React.FC = () => {
    return (
        <div>
            <WebSocketComponent />
        </div>
    );
};

export default Home;
